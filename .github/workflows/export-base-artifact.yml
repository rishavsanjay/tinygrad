name: Export Base Artifact
on:
  push:
    branches: [slice-base-artifact-export]
jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/upload-artifact@v4
        with:
          name: tinygrad-base-artifact
          path: |
            .
            !.git
          include-hidden-files: true
          retention-days: 1
